spring:
  datasource:
    url: jdbc:h2:file:./bdH2/prodDb;DB_CLOSE_DELAY=-1;AUTO_SERVER=TRUE  # Файловая БД
    username: sa
    password: 1
  liquibase:
    contexts: prod
  h2:
    console:
      enabled: true  # Включаем консоль H2