spring:
  datasource:
    url: jdbc:h2:file:./bdH2/testdb;DB_CLOSE_DELAY=-1;AUTO_SERVER=TRUE  # Файловая БД
    username: sa
    password: 1
  liquibase:
    contexts: dev
  h2:
    console:
      enabled: true  # Включаем консоль H2